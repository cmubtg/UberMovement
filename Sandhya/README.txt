The file uber.py contains functions that:
1) Calculates the average ride time for each day of the week
2) Uses SVM to predict the destination ID for a subset of examples
3) Run a t-test on the data to see if the difference between mean average travel time is statistically significant
